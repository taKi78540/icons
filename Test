WeatherDebug_Simplified =
VAR MoisSel =
    MAXX(
        VALUES('TableDate'[YearMonth]),
        'TableDate'[YearMonth]
    )

RETURN
    IF(
        NOT ISBLANK(MoisSel),
        "Visible : " & FORMAT(MoisSel, "YYYY-MM"),
        "Pas de mois sélectionné"
    )
